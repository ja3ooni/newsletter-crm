{
  "name": "ailert-platform",
  "version": "1.0.0",
  "description": "AiLert Professional Newsletter Platform - Microservices Architecture",
  "private": true,
  "workspaces": [
    "services/*",
    "frontend"
  ],
  "scripts": {
    "dev": "docker-compose up -d",
    "dev:stop": "docker-compose down",
    "dev:reset": "docker-compose down -v && docker-compose up -d",
    "dev:logs": "docker-compose logs -f",
    "build": "docker-compose build",
    "build:prod": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml build",
    "test": "npm run test:services && npm run test:integration",
    "test:services": "for dir in services/*/; do (cd \"$dir\" && npm test -- --silent); done",
    "test:integration": "jest --config jest.integration.config.js --silent",
    "test:e2e": "playwright test --reporter=line",
    "test:performance": "k6 run tests/performance/api-load-test.js",
    "test:security": "npm audit --audit-level=moderate && trivy fs .",
    "lint": "npm run lint:services && npm run lint:frontend",
    "lint:services": "for dir in services/*/; do (cd \"$dir\" && npm run lint); done",
    "lint:frontend": "cd frontend && npm run lint",
    "lint:fix": "npm run lint:services:fix && npm run lint:frontend:fix",
    "lint:services:fix": "for dir in services/*/; do (cd \"$dir\" && npm run lint:fix); done",
    "lint:frontend:fix": "cd frontend && npm run lint:fix",
    "format": "npm run format:services && npm run format:frontend",
    "format:services": "for dir in services/*/; do (cd \"$dir\" && npm run format); done",
    "format:frontend": "cd frontend && npm run format",
    "type-check": "npm run type-check:services && npm run type-check:frontend",
    "type-check:services": "for dir in services/*/; do (cd \"$dir\" && npm run type-check); done",
    "type-check:frontend": "cd frontend && npm run type-check",
    "db:migrate": "docker-compose exec user-service npm run db:migrate",
    "db:seed": "docker-compose exec user-service npm run db:seed",
    "db:reset": "docker-compose exec postgres psql -U ailert -d ailert -c \"DROP SCHEMA public CASCADE; CREATE SCHEMA public;\" && npm run db:migrate && npm run db:seed",
    "install:all": "npm install && npm run install:services && npm run install:frontend",
    "install:services": "for dir in services/*/; do (cd \"$dir\" && npm install); done",
    "install:frontend": "cd frontend && npm install",
    "clean": "docker-compose down -v --remove-orphans && docker system prune -f",
    "clean:node_modules": "find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +",
    "health": "curl -f http://localhost:8000/health",
    "logs:user": "docker-compose logs -f user-service",
    "logs:newsletter": "docker-compose logs -f newsletter-service",
    "logs:content": "docker-compose logs -f content-service",
    "logs:crm": "docker-compose logs -f crm-service",
    "logs:analytics": "docker-compose logs -f analytics-service",
    "logs:frontend": "docker-compose logs -f frontend",
    "deploy:staging": "kubectl apply -f k8s/ --context=staging",
    "deploy:prod": "kubectl apply -f k8s/ --context=production",
    "docs:generate": "for dir in services/*/; do (cd \"$dir\" && npm run docs); done",
    "docs:serve": "python3 -m http.server 8080 -d docs",
    "security:scan": "npm audit --audit-level=moderate && trivy fs . --severity HIGH,CRITICAL",
    "security:fix": "npm audit fix",
    "backup": "scripts/backup-dev-data.sh",
    "restore": "scripts/restore-dev-data.sh"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "commander": "^11.1.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.9.0",
    "@types/pg": "^8.10.9",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.54.0",
    "eslint-config-prettier": "^9.1.2",
    "eslint-plugin-prettier": "^5.5.4",
    "husky": "^8.0.3",
    "jest": "^29.7.0",
    "lint-staged": "^15.1.0",
    "prettier": "^3.6.2",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/ailert-platform.git"
  },
  "keywords": [
    "newsletter",
    "ai",
    "microservices",
    "typescript",
    "nextjs",
    "docker",
    "kubernetes"
  ],
  "author": "AiLert Team",
  "license": "MIT",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm run test && npm run lint"
    }
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
